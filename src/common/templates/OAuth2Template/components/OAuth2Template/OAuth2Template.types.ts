import { ReactNode } from 'react';

/**
 * OAuth2Template Props
 */
export type OAuth2TemplateProps = {
    children: ReactNode;
    title: string;
};

/**
 * OAuthTemplate Styles
 */
export type OAuth2TemplateStyles = {
    root: string;
    children: string;
};